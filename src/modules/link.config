MEMORY {
  RAM:
    start = $9ffe
    size  = $2002
    fill  = no;
}

SEGMENTS {
    CODE:   load = RAM, define = yes;
    RODATA: load = RAM, type = ro;
    BSS:    load = RAM, type = bss, define = yes;
}
